Gem::Specification.new do |spec|
  spec.name = "silver-parse-regex-stack"
  spec.version = "0.1.0"
  spec.summary = "Implement a Ruby parsers project for regex simulation kernel, using seeded input scenarios and deterministic summary checks."
  spec.files = Dir["lib/**/*.rb"]
  spec.required_ruby_version = ">= 3.0"
end
